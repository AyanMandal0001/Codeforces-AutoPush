#include <bits/stdc++.h>
using namespace std;


int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int t; cin>>t;
    while(t--) {
        pair<int,int>p;
        cin>>p.first>>p.second;
        if(p.second<-1) cout<<"NO"<<endl;
        else cout<<"YES"<<endl;

        }
   
}